--- kdebase/kcontrol/kcontrol/moduleiconview.cpp	2000/10/10 11:26:05	1.22
+++ kdebase/kcontrol/kcontrol/moduleiconview.cpp	2001/02/22 19:57:41	1.23
@@ -144,6 +144,8 @@
     QString subdir = (*it);
 
     KServiceGroup::Ptr group = KServiceGroup::group(KCGlobal::baseGroup()+subdir+'/');
+    if (!group || !group->isValid())
+       continue;
 
     if (KCGlobal::iconSize() == Small)
     {
