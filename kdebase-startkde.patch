--- kdebase-3.1/startkde.orig	Mon Nov 25 01:45:51 2002
+++ kdebase-3.1/startkde	Tue Nov 26 01:41:07 2002
@@ -5,7 +5,7 @@
 
 # When the X server dies we get a SIGHUP from xinit. We must ignore it
 # because we still need to do some cleanup.
-trap 'echo GOT SIGHUP' SIGHUP
+#trap 'echo GOT SIGHUP' SIGHUP
 
 # Boot sequence:
 #
@@ -62,14 +62,14 @@
 
 usr_odir=$kdehome/share/fonts/override
 usr_fdir=$kdehome/share/fonts
-if test -n "$KDEDIRS"; then
-  kdedirs_first=`echo "$KDEDIRS"|sed -e 's/:.*//'`
-  sys_odir=$kdedirs_first/share/fonts/override
-  sys_fdir=$kdedirs_first/share/fonts
-else
-  sys_odir=$KDEDIR/share/fonts/override
-  sys_fdir=$KDEDIR/share/fonts
-fi
+#if test -n "$KDEDIRS"; then
+#  kdedirs_first=`echo "$KDEDIRS"|sed -e 's/:.*//'`
+#  sys_odir=$kdedirs_first/share/fonts/override
+#  sys_fdir=$kdedirs_first/share/fonts
+#else
+#  sys_odir=/usr/share/fonts/misc/override
+#  sys_fdir=/usr/share/fonts/misc
+#fi
 
 # We run mkfontdir on the user's font dirs (if we have permission) to pick
 # up any new fonts they may have installed. If mkfontdir fails, we still
@@ -77,9 +77,9 @@
 # read-only by the administrator, for whatever reason.
 
 test -d "$usr_odir" && (mkfontdir "$usr_odir" ; xset +fp "$usr_odir")
-test -d "$sys_odir" && xset +fp "$sys_odir"
+#test -d "$sys_odir" && xset +fp "$sys_odir"
 test -d "$usr_fdir" && (mkfontdir "$usr_fdir" ; xset fp+ "$usr_fdir")
-test -d "$sys_fdir" && xset fp+ "$sys_fdir"
+#test -d "$sys_fdir" && xset fp+ "$sys_fdir"
 
 #
 # Add any user-installed font directories to the X font path
