--- kdebase-030421/kicker/menuext/prefmenu/prefmenu.h~	2002-07-09 12:29:18.000000000 +0200
+++ kdebase-030421/kicker/menuext/prefmenu/prefmenu.h	2003-04-23 23:04:00.000000000 +0200
@@ -39,6 +39,8 @@
 protected slots:
     void initialize();
     void slotLaunchControlCenter();
+    void slotLaunchMenuEditor();
+    void slotLaunchDesktopSettingsWizard();
 protected:
     void reload();
 };
--- kdebase-030423/kicker/menuext/prefmenu/prefmenu.cpp~	2002-07-13 20:02:25.000000000 +0200
+++ kdebase-030423/kicker/menuext/prefmenu/prefmenu.cpp	2003-04-24 08:32:03.000000000 +0200
@@ -33,7 +33,7 @@
 
 PrefMenu::PrefMenu(QWidget *parent, const char *name,
                    const QStringList &/*args*/)
-  : PanelServiceMenu( QString::null, QString( "Settings/" ), parent, name)
+  : PanelServiceMenu( QString::null, QString( "KDE-Settings/" ), parent, name)
 {
 }
 
@@ -46,9 +46,12 @@
   if(initialized())
      return;
   
-
   insertItem(SmallIconSet("kcontrol"), i18n("Control Center"),
                this, SLOT(slotLaunchControlCenter()));
+  insertItem(SmallIconSet("kmenuedit"), i18n("Menu Editor"),
+               this, SLOT(slotLaunchMenuEditor()));
+  insertItem(SmallIconSet("kpersonalizer"), i18n("Desktop Settings Wizard"),
+               this, SLOT(slotLaunchDesktopSettingsWizard()));
   insertSeparator();
 
   // now initialize the menu itself
@@ -62,6 +65,16 @@
                                           0, 0, 0, "", true);
 }
 
+void PrefMenu::slotLaunchMenuEditor() {
+  KApplication::startServiceByDesktopName("kmenuedit", QStringList(), 
+                                          0, 0, 0, "", true);
+}
+
+void PrefMenu::slotLaunchDesktopSettingsWizard() {
+  KApplication::startServiceByDesktopName("kpersonalizer", QStringList(), 
+                                          0, 0, 0, "", true);
+}
+
 void PrefMenu::reload()
 {
 	initialize();
